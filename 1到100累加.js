/* 1到100累加 */

/* 要进行100次的循环才能计算出结果 */
var j=0;
for(var i=0;i<=100;i++){
  j+=i;
}
console.log(j);

/* 等差数列 不需要循环，只是根据等差数列进行运算 */
var i = 1,sum = 0,n = 100;
sum = ( 1 + n ) * n / 2;
console.log("sum",sum);

/* 速算法 37+45*/
var res = 37 + (43 + 2); //37 + 43 +2
console.log(res);
/* 将45进行拆分成 43 + 2 这样可以快速算出整数80 再加上2 */

/* 速算法 83-37*/
var res2 = 83 - (33 + 4); // (83 - 33) - 4
console.log(res2);